# Everything Phase 0 needs, on one index.
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# torch: pick a build your driver supports.  A driver reporting CUDA 12.8 runs
# any cu12x build at or below 12.8; it cannot run a CUDA 13 build.  Check with
#   nvidia-smi --query-gpu=driver_version --format=csv
torch>=2.6,<2.12

# transformers 5.x removed APIs that vLLM still calls
# (Qwen2Tokenizer.all_special_tokens_extended) and renamed torch_dtype -> dtype.
# The default `hf` backend works on 4.x or 5.x; vLLM needs 4.x.
transformers>=4.51,<5

reasoning-gym>=0.1.25
numpy>=1.26
matplotlib>=3.8

# Optional, for --backend vllm only.  Install separately: the wheel must be
# compiled against a CUDA version your driver supports, which pip cannot infer.
# vllm
