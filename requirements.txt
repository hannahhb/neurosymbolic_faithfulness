# Core analysis (laptop + GPU box)
torch>=2.4
transformers>=4.45
datasets>=3.0
numpy>=1.26
scikit-learn>=1.4
transformer-lens>=2.0   # activation harvesting (nsf/activations.py)
pandas>=2.0
matplotlib>=3.8

# Generation on the GPU box only. vLLM has no macOS/MPS build; the HF backend
# in nsf.generate covers local smoke tests.
# vllm>=0.6.0
